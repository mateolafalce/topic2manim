# API Keys - Configure at least one
# Priority 1: Claude (if available, will be used first)
CLAUDE_API_KEY=
# Priority 2: OpenAI (fallback if Claude is not configured)
OPENAI_API_KEY=

# Model Configuration
CLAUDE_MODEL=claude-sonnet-4-5-20250929
OPENAI_MODEL=gpt-5.2

# TTS Configuration (requires OpenAI API key)
TTS_MODEL=gpt-4o-mini-tts
VOICE=alloy

# Video Topic
TOPIC=how chat gpt works?